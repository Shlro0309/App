# Kiểm thử

Thư mục này dùng cho tài liệu kiểm thử cấp dự án, test tích hợp toàn hệ thống và test end-to-end trong các giai đoạn sau.

Hiện tại, test backend vẫn đặt theo chuẩn Maven tại `src/backend/src/test`.
