# Tài nguyên tĩnh

Thư mục này dùng cho tài nguyên tĩnh cấp dự án mà người dùng có thể truy cập trực tiếp khi được cấu hình phục vụ bởi web server.

Với frontend Vite, tài nguyên tĩnh riêng của ứng dụng có thể đặt trong `src/frontend/public` khi cần.
