# Cấu hình dự án

Thư mục này dùng để lưu tài liệu và file cấu hình mẫu cho môi trường triển khai.

Các file cấu hình thật có thông tin nhạy cảm không được commit lên Git. Với backend, tạo file runtime từ `src/backend/.env.example` thành `src/backend/.env`.
